import { FormatoAutoresPipe } from './formato-autores.pipe';

describe('FormatoAutoresPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoAutoresPipe();
    expect(pipe).toBeTruthy();
  });
});
